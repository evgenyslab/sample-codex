[phases.setup]
nixPkgs = ['python310', 'nodejs-18_x', 'libsndfile']

[phases.install]
cmds = [
    'cd frontend && npm install && npm run build',
    'cd backend && pip install -e .'
]

[start]
cmd = 'cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT'
